import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center',
        flex: 1,
        height: 600,
    },
    title: {
        color: '#ECECEC',
        fontSize: 100,
        marginBottom: 20
    },
    subTitle: {
        color: '#ECECEC',
        fontSize: 20
    } 
})